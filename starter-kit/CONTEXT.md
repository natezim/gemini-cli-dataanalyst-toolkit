# Project Context

Written by `/start`. Update with `/context:update`.

Drop reference files (specs, docs, schemas) into `./context/` — auto-loaded every session.
WARNING: NEVER put credentials, .env, service account keys, or PII in `./context/`.

## Learned (auto)

<!--
Gemini appends durable project facts here as it learns them, format:
- [YYYY-MM-DD] <fact> — source: <session or file ref>

Prune or reorganize anytime. This section is curated by Gemini;
the rest of this file is curated by you.
-->
